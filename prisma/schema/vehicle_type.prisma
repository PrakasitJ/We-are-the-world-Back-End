model Vehicle_type {
    id           Int      @id @default(autoincrement())
    wheel        Int
    vehicle_type String   @db.VarChar(15)
    createdAt    DateTime @default(now())
    updatedAt    DateTime @updatedAt

    Rider Rider[]
}
